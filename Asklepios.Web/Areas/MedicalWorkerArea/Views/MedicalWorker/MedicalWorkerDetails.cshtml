
@model Asklepios.Web.Areas.MedicalWorkerArea.Models.MedicalWorkerViewModel
@{ 
    MedicalWorker md = Model.MedicalWorker;
}
<div class="d-flex  flex-column jumbotron w-100 h-auto align-content-between" style=" padding:20px;">
    <div class="d-flex mb-3 flex-wrap  align-items-center">
        <div class="p-2  pt-3 align-content-center align-items-center align-self-center mx-auto" style="width:400px; text-align:center">
            <div class="d-flex flex-fill  justify-content-center" style="text-align:center">
                <img alt="@md.FullProffesionalName" src="@md.Person.ImageSource" class="img-fluid" style="display: block; max-height:300px;
        border-radius: 6px; ">
            </div>
        </div>
        <div class="d-flex  p-2 flex-grow-1 pt-3  align-content-stretch align-items-center" style="width:400px; text-align:center">
            <div class="d-flex flex-column m-auto">
                <h3 style="font-weight:900;">
                    @md.FullProffesionalName
                </h3>
                <br />
                <h5>Region - @md.Person.DefaultAglomeration</h5>
                <h5>Edukacja:</h5>
                @*@for (int i = 0; i < md.MedicalWorker.Education.Count; i++)
                {*@
                    <p>@md.Education </p>
                @*}*@
                <h5>Doświadczenie:</h5>
                <div>@md.Experience</div>
            </div>
        </div>

        <div class="p-2 flex-grow-1 pt-3 mx-auto flex-grow-1 " style="width:400px;text-align:center">
            <h3 style="font-weight:900;"> </h3>
            <h5>Zakres specjalizacji:</h5>
            <h5>
                @for (int i = 0; i < md.MedicalServices.Count; i++)
                {
                    MedicalService service = md.MedicalServices[i];
                    //if (i % 2 == 1)
                    //{
                        <br>
                    @*}*@
                    @service.Name
                }

            </h5>
            <partial name="_Stars" model="@md.AverageRating" />

            @if (md.VisitReviews.Count > 0)
            {
                @md.RatingDescription
            }
        </div>
    </div>
    <div  class="d-flex flex-column">
        <h2>Opinie</h2>
        <div class="d-flex flex-column">
            @for (int i = 0; i < md.VisitReviews.Count; i++)
            {
                VisitReview review = md.VisitReviews[i];
                if (i % 2 == 0)
                {
            <div class="d-flex mt-2">
                <partial name="_MedicalReview" model="@review" />
            </div>
                    
                }
                else
                {
        <div class="d-flex mt-4">
            <partial name="_MedicalReview" model="@review" />
        </div>
                    
                }


            }
        </div>
    </div>
</div>
